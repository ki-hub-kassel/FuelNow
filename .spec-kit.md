# FuelNow — Spec-Kit Hub (lokal)

Diese Datei ist der **Einstieg** für Spec-Driven Arbeit: *was gilt, wo steht die Detail-Spezifikation*. [GitHub Spec-Kit](https://github.com/github/spec-kit) ist **optional** — vollständige CLI-Einrichtung (Ordner `.specify/`, Cursor-Integration) steht in **[`docs/SPECKIT.md`](docs/SPECKIT.md)**.

**Status:** **`.specify/` ist aktiv** (Specify CLI, Integration `cursor-agent`). Produkt-Specs: `docs/PRODUCT_SPEC.md` usw.; Spec-Kit-Verfassung: **`.specify/memory/constitution.md`** (mit FuelNow-Daten befüllt).

## Kanonische Spezifikation

| Rolle | Pfad |
| --- | --- |
| **Spec-Kit CLI & Cursor-Integration** | [`docs/SPECKIT.md`](docs/SPECKIT.md) |
| **Produkt-Index (Single Source für Scope & Konstanten)** | [`docs/PRODUCT_SPEC.md`](docs/PRODUCT_SPEC.md) |
| **Abo / Plus / TestFlight / Sandbox** | [`docs/SUBSCRIPTIONS.md`](docs/SUBSCRIPTIONS.md) |
| **CarPlay-Modi & Marketing-Kohärenz** | [`docs/CARPLAY.md`](docs/CARPLAY.md) |
| **Agent-Präferenzen & Kurzfakten** | [`AGENTS.md`](AGENTS.md) |
| **Tankerkönig API (Decoding, Limits)** | [`.cursor/skills/tankerkoenig-api/SKILL.md`](.cursor/skills/tankerkoenig-api/SKILL.md) |

## Harte Fakten (Kurz)

- **Produkt / Target:** FuelNow — **Repo-Pfad** historisch `TankRadar` (Mismatch bewusst).
- **Bundle-ID:** `com.vibecoding.fuelnow`
- **App Store:** Verfügbarkeit **nur Deutschland** (Metadaten/ASC; siehe `docs/AppStoreConnectUpload.md`).
- **Plus SKUs:** `com.vibecoding.fuelnow.subscription.year` und `com.vibecoding.fuelnow.subscription.month` — `SubscriptionConstants` + `FuelNowPlus.storekit`.
- **Tankerkönig:** Default über Vercel-Proxy (`TankerkoenigProxyBaseURL` in `Info.plist`); Key nicht im Binary (TAN-92).

## Spec-Kit / `.specify` nachziehen

Schritt-für-Schritt: **[`docs/SPECKIT.md`](docs/SPECKIT.md)** (`brew install uv` → `uv tool install specify-cli …` → `specify init --here --integration cursor-agent --ignore-agent-tools`). Danach hier **einen Satz** ergänzen: „`.specify/` aktiv (Datum)“.

## Pflege

- Nach Linear-/Produktänderungen: zuerst **`docs/PRODUCT_SPEC.md`** und ggf. **`AGENTS.md`** anpassen.
- Diese Datei nur ändern, wenn sich **Einstiegspfade** oder **Kurzfakten** ändern (nicht jedes Ticket duplizieren).
